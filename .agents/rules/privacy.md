# Privacy Rules

- Do NOT read, view, or search the contents of any `.env` files in this workspace. 
- Do NOT output, mention, or log any sensitive API keys or credentials. 
- If you need to know if a specific environment variable is present, check the application code or configuration instead, or ask the user to verify it themselves.
